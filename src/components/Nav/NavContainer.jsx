import styled from "styled-components"

const NavContainer = styled.nav`
  display: flex;
  align-items: center;
  height: min-content;
  padding: 3rem 0;
  z-index: 3000;
`

export default NavContainer
